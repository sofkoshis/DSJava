public class AverageCalc {
    //mehotd that takes number of double values
    public static double average;
    double values;
    double sum = 0;
    for(double i : values) {
        sum += i;
    }
    return values.length > 0;
    sum/values.length : 0;
    System.out.println("Average of 10,8,9:" + average(10,8,9));
    System.out.println("Average of all entered scores: " + average(Arrays.copyOf(scores, count)));
    average(Arrays.copyOf(scores, count));
}
